import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
        primary: {
            main: "#69bf97",
            dark: "#579f7d",
            light: "#b1ecd3",
            contrastText: "#ffffff",
        },
    },
});

export default theme;